import React from "react";

const NotFound = () => {
  return (
    <div style={{ flexGrow: "1", backgroundColor: "red", color: "white" }}>
      NotFound
    </div>
  );
};

export default NotFound;
